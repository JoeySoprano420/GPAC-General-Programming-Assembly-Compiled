#include "gpac_zvm_runtime.hpp"

// GPAC ZVM runtime implementation unit.
// Most primitives are inline templates for easy embedding.
// Native bridge adapters are registered here in larger builds.
