-- structs.gpac
Main: program = structs_demo

struct Person
    name: string
    age: int
end

Proc main()
    let p: Person
    p.name = "Joey"
    p.age = 30

    print p.name -> console
    print p.age -> console
end
